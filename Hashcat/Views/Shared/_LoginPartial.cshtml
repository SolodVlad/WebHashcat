@using Domain.Models;
@using Microsoft.AspNetCore.Identity
@using System.Security.Claims;
@using System.IdentityModel.Tokens.Jwt;
@*@inject SignInManager<User> SignInManager*@
@*@inject UserManager<User> UserManager*@

@*@{
    var token = Context.Request.Cookies["AuthCookie"];
    if (!string.IsNullOrEmpty(token))
    {
        var handler = new JwtSecurityTokenHandler();
        var jwtSecurityToken = handler.ReadJwtToken(token);
        if (!string.IsNullOrEmpty(jwtSecurityToken.Claims.First(claim => claim.Type == ClaimTypes.Name).Value))
        {
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@jwtSecurityToken.Claims.First(claim => claim.Type == ClaimTypes.Name).Value</a>
            </li>
            <li class="nav-item">
                <input type="button" id="logoutBtn" class="nav-link btn btn-link text-dark" value="Вихід"/>
            </li>
        }
    }
    else
    {
        <li>
            <a href="@Url.Action("Login", "Account", new {Area = "Identity"})">Авторизація</a>
        </li>
    }
}*@

@*@{
    string email = ViewBag.email;
    if (!string.IsNullOrEmpty(email))
    {
        <li class="nav-item">
            <a id="showEmail" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@email</a>
        </li>
        <li class="nav-item">
            <input type="button" id="logoutBtn" class="nav-link btn btn-link text-dark" value="Вихід" />
        </li>
    }
    else
    {
        <li>
            <a href="@Url.Action("Login", "Account", new {Area = "Identity"})">Авторизація</a>
        </li>
    }
}*@

<li class="nav-item" id="balanceLi" style="display: none;">
    <a id="showBalance" class="nav-link"></a>
</li>

<li class="nav-item" id="emailLi" style="display: none;">
    <a id="showEmail" class="nav-link" href="@Url.Action("Index", "Home", new {Area = "Cabinet"})" title="Кабінет"></a>
</li>

<li class="nav-item" id="logoutLi" style="display: none;">
    <a id="logoutBtn" class="nav-link" href="#" >
        <i class="fas fa-door-open"></i>
    </a>
</li>

<li class="nav-item" id="loginLi">
    <a class="nav-link visual-a-blue" href="@Url.Action("Login", "Account", new {Area = "Identity"})">Авторизація</a>
</li>